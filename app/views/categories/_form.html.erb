<%= form_with(model: category, class: "p-2") do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-1">
    <%= form.label :name, style: "display: none" %>
    <%= form.text_field :name, class:"form-control", placeholder: "Name"  %>
  </div>

  <div class="mb-1">
    <%= form.label :icon, style: "display: none" %>
    <%= form.text_field :icon, class:"form-control", placeholder: "Icon URL"  %>
  </div>

  <div class="position-absolute bottom-0 start-50 translate-middle-x vw-100 second p-3">
    <div class="border border-light text-center">
          <%= form.submit "Create Category", class:"navbar-brand border-0 second text-white"  %>
    </div>
  </div>

<% end %>
